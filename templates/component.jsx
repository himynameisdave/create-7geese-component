import React from 'react';
import PropTypes from 'prop-types';
import '{kebab}.less';


const {pascal} = () => (
    <div className="{kebab}">
        {pascal}
    </div>
);

{pascal}.propTypes = {};
{pascal}.defaultProps = {};

export default {pascal};
